var = 'var = %r # Using %%r for repr yielding the raw string\nprint(var %% var) # Printing var with var as an argument to %%r' # Using %r for repr yielding the raw string
print (var % var) # Printing var with var as an argument to %r
